{
  "name": "arcade",
  "private": true,
  "version": "1.0.0",
  "description": "Local dev server for the Pazneria arcade landing page.",
  "scripts": {
    "dev": "node scripts/dev-server.js --open",
    "start": "node scripts/dev-server.js --open",
    "serve": "node scripts/dev-server.js",
    "test": "node scripts/test-wiki-link-contract.js"
  }
}
